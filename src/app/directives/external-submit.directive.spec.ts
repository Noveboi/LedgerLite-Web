import { ExternalSubmitDirective } from './external-submit.directive';

describe('ExternalSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
